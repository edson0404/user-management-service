/*
package com.blackswan.test;

import com.blackswan.test.task.TaskRepository;
import org.junit.Before;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.boot.test.autoconfigure.orm.jpa.TestEntityManager;
import org.springframework.test.context.junit4.SpringRunner;

//import com.blackswan.test.user.User.UserId;

@RunWith(SpringRunner.class)
@DataJpaTest
public class TaskRepositoryTest {

    @Autowired
    TaskRepository taskRepository;

    @Autowired
    UserRepository userRepository;

    @Autowired
    TestEntityManager entityManager;

//    UserId janeUserId;

//    UserId johnUserId;

    @Before
    public void createUsers() {
//        janeUserId = new UserId((Long) entityManager.persistAndGetId(new User("janed", "Jane", "Doe")));
//        johnUserId = new UserId((Long) entityManager.persistAndGetId(new User("johnd", "John", "Doe")));
    }

    */
/*@Test
    public void testCreateTask() {
        Task task = new Task(userRepository.findById(janeUserId.getUserId()).get());
        TaskItem taskItem = new TaskItem("Task1", "Task1 Description", new Date());
        task.addTask(taskItem);

        entityManager.persist(task);

        assertThat(taskRepository.findAll()).contains(task);

    }*//*

}
*/
